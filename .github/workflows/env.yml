on: 
  push:
    branches:  tanishchoudhary-yoyo_1
    
jobs:
  bro:
  
    runs-on: ubuntu-latest
    
    
    steps:
      - name: 'checkout coode ${{ github.REF }}'
        uses: actions/checkout@v2.4.0
      - name: 'yoo'
        run: |
          echo "hello"
          #cat ./test.json
          jq '.workflow_runs[].id' ./test.json
